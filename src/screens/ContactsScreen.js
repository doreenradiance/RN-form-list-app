import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

export default function ContactsScreen({route,navigation}) {
    let ContactsScreen = route.params
    return (
        <View>
            <Text></Text>
        </View>
    )
}

const styles = StyleSheet.create({})
